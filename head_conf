/*
   该文件包含了需要搜索的.h目录
   以执行该脚本的目录为起点，写入需要包含H文件的目录, 编译的时候，
   会自动搜索该目录下的所有h文件，然后形成h文件列表, 配置方式为:

   module 1
   include
   port/GCC 


   不支持直接配置文件, 默认会先搜索当前目录，然后再搜索配置目录。
   如果没有配置文件，会把当前目录下的所有目录都查找一遍，记录下
   所有的h文件。配置项后面的数字，目的在于指定当前目录的深度，不
   指定则为99
*/
